import { Component } from '@angular/core';

@Component({
  selector: 'app-carousel', // Asegúrate de que este selector coincida con el que usas en tu HTML
  templateUrl: './carousel.component.html', // Ruta al archivo HTML del carousel
  styleUrls: ['./carousel.component.css'] // Ruta al archivo CSS (si lo tienes)
})
export class CarouselComponent {

}